# Admin panel UI
1. Install dependencies:
    ```
    npm install
    ```
2. Build

    a. Run locally in watch mode

      ```
      npm run dev
      ```

    b. Make a build

      ```
      npm run build
      ```