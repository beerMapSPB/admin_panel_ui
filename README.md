# Admin panel UI
Install dependencies
```
npm install
```

### To launch locally

1. Build and serve in launch mode
    ```
    npm run dev
    ```
2. Run json-server to mock requests to back-end
    ```
    npm run mock-server
    ```

### To make a build

```
npm run build
```